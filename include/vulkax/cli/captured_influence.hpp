#pragma once

namespace vulkax::cli {

// Returns -1 when argv does not request the captured material influence command.
int capturedInfluenceCommand(int argc, char** argv);

} // namespace vulkax::cli
