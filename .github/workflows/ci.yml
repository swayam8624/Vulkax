name: Vulkax Next CI

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI temporarily paused during an atomic repository migration; push/PR matrix restored in the final commit."
